# Smart Placement AI - Environment Configuration
# ============================================

# REQUIRED: Get your free key from https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (defaults work fine, change if needed)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=placement_db

# MinIO Object Storage (defaults work fine)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Redis Cache (defaults work fine)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Note: Google OAuth is pre-configured in the frontend!
# Just run and sign in with any Gmail account - no setup needed.
# For production, replace CLIENT_ID in frontend/src/main.tsx
